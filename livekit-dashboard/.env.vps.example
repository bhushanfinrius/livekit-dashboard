# VPS — copy to .env when livekit-dashboard and agent-starter-python are sibling folders on the server.
# Upload both folders via SFTP/rsync/scp (no git required).

DATABASE_URL="postgresql://deck:deck@postgres:5432/deck"
AUTH_SECRET="REPLACE_WITH_openssl_rand_base64_32"
AUTH_URL="http://103.191.209.120:3000"
ENCRYPTION_KEY="REPLACE_WITH_openssl_rand_base64_32"
DECK_TRANSCRIPT_SECRET="REPLACE_WITH_random_32_chars!!!!!!!!"

# Public IP or hostname for LiveKit TURN + ICE (used by npm run docker:vps:up)
LIVEKIT_PUBLIC_IP="103.191.209.120"

# Sibling folder on VPS (default layout):
AGENT_BUILD_CONTEXT="../agent-starter-python"

# Agent secrets live in ../agent-starter-python/.env.local (never commit).
# Required files on VPS (not in git):
#   ../agent-starter-python/solvoxai.json
#   ../agent-starter-python/livekit-storage.json

# Generate secrets on VPS:
#   openssl rand -base64 32
